{
  "name": "chatynkowo",
  "version": "1.0.0",
  "private": true,
  "description": "Chatynkowo — baśniowa mapa ukrytych chatynek Elfów",
  "scripts": {
    "build": "bash scripts/build.sh",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build && npm run serve"
  }
}
